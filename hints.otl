general
	--gameplane will occupy all space we have(COLSxROWS)
	--first we got menu {exit|start|...}

TODO: funkce by nemely byt volany (row-2, col-2), ale (COORDS c) ...
switches in menu

AI: cf = get_closest_fruit();
	go_to_closest_fruit();		// sets next_dirrection in snake object
	
